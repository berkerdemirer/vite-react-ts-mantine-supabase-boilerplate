import React from "react";

const Secure: React.FC = () => {
  return (
    <div>
      <h1>This is a secure page</h1>
      <p>Only authenticated users should be able to see this.</p>
    </div>
  );
};

export default Secure;
